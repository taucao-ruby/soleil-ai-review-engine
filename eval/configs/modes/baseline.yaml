# Baseline mode — no soleil-ai-review-engine, pure mini-swe-agent (control group)
agent:
  agent_class: "eval.agents.soleil-ai-review-engine_agent.soleil-ai-review-engineAgent"
  soleil-ai-review-engine_mode: "baseline"
  step_limit: 30
  cost_limit: 3.0

environment:
  environment_class: "docker"
